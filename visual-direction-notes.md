# Direção visual inicial — 23/09/2026

A cópia isolada parte do commit `6ea70cd` da `main` e mantém 123 mídias rastreadas. A inspeção dos vídeos mostrou que `video1.mp4` é uma peça vertical de dados sobre crescimento de vendas e `video2.mov` é uma peça vertical narrativa com pessoas subindo uma montanha. Ambos devem permanecer preservados dentro da Caixa-Preta/área editorial, mas não serão usados como background principal da Home porque o prompt pede um hero abstrato, silencioso e sem pessoas ou texto pré-incorporado.

A Home será reestruturada como experiência editorial escura com camadas CSS/SVG leves, linhas de scan, glow laranja controlado, dados grandes e conteúdo preservado. O vídeo continuará disponível nos módulos existentes, com lazy loading e fallback.
